# Performance Report — 2026-03-27 (Morning)

**Agent:** Performance  
**Site:** tinytoolbox.co  
**Run:** 2026-03-27 08:15 AM ET

---

## Scores Summary

| Metric | Mobile | Desktop | Yesterday (Desktop est.) | Change |
|--------|--------|---------|--------------------------|--------|
| Performance | 96 | 45 ⚠️ | ~72–78 | **-27 pts** 🚨 |
| Accessibility | 73 ⚠️ | 73 ⚠️ | ~96 | **-23 pts** 🚨 |
| Best Practices | 100 ✅ | 100 ✅ | ~100 | — |
| SEO | 100 ✅ | 100 ✅ | ~100 | — |

## Core Web Vitals

### Mobile
| Vital | Value | Rating |
|-------|-------|--------|
| FCP | 2.0s | Needs Improvement |
| LCP | Error (NO_LCP) | **POOR** 🚨 |
| CLS | 0.0 | Good |
| TBT | Error (NO_LCP) | **POOR** 🚨 |

### Desktop
| Vital | Value | Rating |
|-------|-------|--------|
| FCP | 0.5s | Good |
| LCP | 1.1s | Needs Improvement |
| CLS | 0.707 | **POOR** 🚨 |
| TBT | 780ms | Needs Improvement |

---

## ISSUES FOUND

### 🚨 CRITICAL: Desktop Performance crashed to 45 (was ~72-78)
- Score dropped ~27 points overnight — severe regression
- Top factors: Render blocking requests (310ms savings), Legacy JS (78 KiB), unoptimized DOM

### 🚨 CRITICAL: Mobile LCP failure (NO_LCP)
- Largest Contentful Paint completely failed to render
- Likely caused by lazy-loaded hero images or missing LCP image hints

### 🚨 HIGH: Accessibility score dropped to 73 (was ~96)
- Primary issue: **Contrast** — background and foreground colors do not have sufficient contrast ratio
- 10 manual checks also recommended

### ⚠️ MODERATE: Desktop CLS at 0.707 (Google threshold for "poor" is >0.25)
- Layout shift culprits identified on desktop
- Causes: forced reflow, network dependency chain

### ⚠️ MODERATE: Desktop TBT at 780ms
- 13 long main-thread tasks found
- Main thread work: 3.1s
- 5 non-composited animations

---

## Recommendations

### 1. Fix LCP on Mobile (CRITICAL)
**Fix:** Add `fetchpriority="high"` to hero/LCP image, add `preload` link tag for the LCP image, ensure hero image is not lazy-loaded.

### 2. Fix Desktop Performance (CRITICAL)
**Fix:** 
- Eliminate render-blocking requests (inline critical CSS or defer non-critical)
- Remove or replace Legacy JavaScript (transpile to modern ES2020+)
- Enable browser caching for static assets (147 KiB savings from cache lifetimes)

### 3. Fix Accessibility Contrast (HIGH)
**Fix:** Review glass-panel, footer, and text elements. Increase contrast ratio to WCAG AA (4.5:1 for normal text). Check `glass-panel` class in `globals.css` — `bg-white/5` over dark background likely fails contrast.

### 4. Fix Desktop CLS (MODERATE)
**Fix:** Set explicit `width`/`height` on all images. Review layout shift culprits — likely from dynamically injected content or ads. Add `aspect-ratio` CSS to media containers.

### 5. Reduce TBT on Desktop (MODERATE)
**Fix:** Code-split JavaScript bundles, defer non-critical JS, reduce main-thread work (3.1s → target <200ms).

---

## Failing Audits (from Lighthouse)

### Desktop
- `render-blocking-resources` — 310ms savings available
- `unused-javascript` — 119 KiB savings
- `legacy-javascript` — 78 KiB of polyfills
- `non-composited-animations` — 5 animated elements
- `long-tasks` — 13 long tasks found
- `dom-size` — Optimize DOM size

### Mobile (same +)
- `unused-css` — Minify CSS recommended
- `unused-javascript` (more severe on mobile)

### Both
- `contrast` — Insufficient color contrast
- `deprecated-apis` — 1 deprecated API warning
- `errors-in-console` — Browser errors logged

---

*Generated by Performance Agent — next run at 8pm ET*
